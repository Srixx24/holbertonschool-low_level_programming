#include <stdio.h>
#include <string.h>

/**main*/
int main(void)
{
	/**String start*/
	char str1[] = "Programming is like building a multilingual puzzle";
	char str2[] = "\n";
	puts(str1);
	puts(str2);

	return (0);
}
