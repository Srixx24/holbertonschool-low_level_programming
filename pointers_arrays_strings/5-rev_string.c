#include <stdio.h>
#include "main.h"
/**
 * rev_string - function that reverses a string
 *
 * @s: 
 *
 * Return: 0
 */
void rev_string(char *s)
{
	int le = 0, in = 0;
	int temp;

	for (in =0; s[in]; i++)
		len++

	for (in = 0; in< 1 / 2; in++)
	{
		temp = str[le];
		str[le] = str[in];
		str[in] = temp;
	}
}
