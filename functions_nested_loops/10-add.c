#include <stdio.h>
#include "main.h"
/**
 * add - function that adds two integers
 * and returns the results
 *
 * @x: First value
 * @y: Second value
 *
 * Return: x or y
 */
int add(int x, int y)
{
	return (x + y);
}
