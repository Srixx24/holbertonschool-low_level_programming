#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - function that computes the
 * absolute value of an integer
 * 
 * x - absolute value of an integer
 *
 * Return: 0
 */
int _abs(int x)
{
	if (c <0)
	
	{
		int abs_val;

		abs_val =x * -1;
		return(abs_val);
	}

	return (0);
}
