#include <stdio.h>
#include "main.h"
/**
 * islower - Writing a function that checks
 * for lowercase characters that returns 1
 * if c is lowercase otherwise 0
 *
 * Return: 0 or 1
 */

int _islower(int c);
({
	char 'x';

	if (islower(ch) ! = 0
		{
		printf("The given character is a lowercase letter.\n");
		}
	else 
		{
		("The given character is not a lowercase letter.\n");
		}
	return (0);
}
